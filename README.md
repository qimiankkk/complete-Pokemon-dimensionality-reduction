# complete-Pok-mon-dimensionality-reduction
This dashboard applies nonlinear dimensionality reduction to the complete Pokémon dataset, compressing 41 features into a 2D latent space via a configurable PyTorch Lightning autoencoder. The latent space is then clustered (K-Means and HDBSCAN), rendered as an interactive WebGL scatter plot.
